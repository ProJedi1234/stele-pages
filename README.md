# stele-pages

Publish an HTML file, get a readable link back.

A small Swift server that stores pages in Postgres and serves them at three-word slugs
like `quiet-cedar-otter` — unique without being a UUID.
